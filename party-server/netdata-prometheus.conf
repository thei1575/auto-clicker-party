# Netdata Go.d Prometheus collector job for Auto Clicker Party.
# Copy this job into /etc/netdata/go.d/prometheus.conf on the relay host.
jobs:
  - name: auto_clicker_party
    url: http://127.0.0.1:8787/metrics
    update_every: 5
    expected_prefix: auto_clicker_party_
